#include<iostream>
#include"Price.h"
int main(){
    Price p(1,20);
    Price p1(1);
    // std::cout<<p1++<<std::endl;
    std::cout<<(double)p<<std::endl;
    // p = 2;
    // float incomeCurrent = 100000000.00f;
    // float incomeForExit = 100000010.00f;
    // unsigned int years = 0;
    // for (float income = incomeCurrent; income <= incomeForExit; ++income)
    //     ++years;
    // std::cout<<"Wait " <<years << "for exit!" << std::endl;
    
    // float bunPrice = 1.1f;
    // float pric = 1.1f;
    //  unsigned int bunsNum = 0;
    //  for (float budget = 22.0f; budget != 0; budget -= pric)
    //     ++b unsNum;

    // Price p1 = Price(0,12);
    // Price p2 = Price(5,12);
    
    // std::cout<< p1<<std::endl;
    // ++p1;
    // p1++;
    // std::cout<<p1<<std::endl;
    // std::cout<<--(-p1)<<std::endl;
    // std::cout<<++(-p1)<<std::endl;
    
    return 0;
}